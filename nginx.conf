server {
    listen 80;

    root /app/dist;

    # maplibre tile worker — nginx default mime.types has no .mjs mapping
    location ~* \.mjs$ {
        default_type application/javascript;
    }

    location / {
        index index.html;
        try_files $uri $uri/ /index.html;
    }
}
